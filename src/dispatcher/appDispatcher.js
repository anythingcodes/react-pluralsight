var Dispatcher = require('flux').Dispatcher;
// holds a list of callbacks, and stores register with this dispatcher to say they'd like to be informed by this dispatcher

module.exports = new Dispatcher();
